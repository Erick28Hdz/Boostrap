<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Dashboard - <%= usuario.nombre %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      min-height: 100vh;
      display: flex;
    }

    .sidebar {
      width: 250px;
      background-color: #343a40;
      color: white;
      flex-shrink: 0;
    }

    .sidebar a {
      color: white;
      text-decoration: none;
      padding: 12px 20px;
      display: block;
    }

    .sidebar a:hover {
      background-color: #495057;
    }

    .main-content {
      flex-grow: 1;
      padding: 30px;
      background-color: #f8f9fa;
    }

    .user-info p {
      margin-bottom: 8px;
    }
  </style>
</head>
<body>

  <!-- Sidebar -->
  <%- include('./partials/sidebar') %>

  <!-- Contenido principal -->
  <div class="main-content">
    <h2 class="mb-4">Bienvenido, <%= usuario.nombre %></h2>

    <div class="card shadow-sm user-info">
      <div class="card-header bg-primary text-white">
        Datos del usuario
      </div>
      <div class="card-body">
        <p><strong>Nombre:</strong> <%= usuario.nombre %></p>
        <p><strong>Email:</strong> <%= usuario.email %></p>
        <p><strong>Rol:</strong> <%= usuario.rol.nombre %></p>

        <% if (usuario.rol.nombre === 'admin') { %>
          <div class="alert alert-info mt-4">
            Eres administrador. Puedes acceder a funciones avanzadas.
          </div>
        <% } else { %>
          <div class="alert alert-success mt-4">
            Eres usuario regular. Bienvenido a tu panel.
          </div>
        <% } %>
      </div>
    </div>
  </div>

</body>
</html>
