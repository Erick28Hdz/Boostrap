<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8" />
  <title>Centro de Publicidad y Promociones - Admin</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      min-height: 100vh;
      display: flex;
      background-color: #f1f3f5;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .sidebar {
      width: 250px;
      background-color: #343a40;
      color: white;
      flex-shrink: 0;
    }

    .sidebar a {
      color: white;
      text-decoration: none;
      padding: 12px 20px;
      display: block;
    }

    .sidebar a:hover {
      background-color: #495057;
    }

    .main-content {
      flex-grow: 1;
      padding: 30px;
    }

    .form-section {
      background-color: white;
      border-radius: 12px;
      padding: 25px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.06);
    }

    .form-section h2 {
      margin-bottom: 20px;
    }

    .card-publicidad {
      border: none;
      border-radius: 12px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.05);
      background-color: white;
      transition: transform 0.2s ease;
    }

    .card-publicidad:hover {
      transform: scale(1.02);
    }

    .card-publicidad img {
      border-top-left-radius: 12px;
      border-top-right-radius: 12px;
      max-height: 200px;
      object-fit: cover;
    }

    .card-body {
      padding: 15px 20px;
    }

    .card-title {
      font-size: 1.1rem;
      font-weight: bold;
    }

    .preguntas-frecuentes {
      background-color: #ffffff;
      padding: 20px;
      border-radius: 12px;
      margin-bottom: 40px;
      box-shadow: 0 0 8px rgba(0, 0, 0, 0.04);
    }
  </style>
</head>

<body>

  <!-- Sidebar -->
  <%- include('../partials/sidebar') %>

  <!-- Contenido principal -->
  <div class="main-content container-fluid">
    <h1 class="mb-4">🎯 Centro de Publicidad y Promociones</h1>

    <!-- Sección de Preguntas Frecuentes -->
    <section class="preguntas-frecuentes">
      <h2 class="mb-3">📌 Preguntas Frecuentes</h2>
      <div id="lista-preguntas">
        <p>Próximamente se mostrarán preguntas frecuentes sobre la gestión de publicidad.</p>
      </div>
    </section>

    <!-- Sección del Formulario -->
    <div class="form-section mb-5">
      <h2>📢 Subir Nueva Publicidad</h2>
      <form action="/admin/publicidad/nueva" method="POST" enctype="multipart/form-data">
        <div class="mb-3">
          <label class="form-label">Imagen (JPG/PNG)</label>
          <input type="file" name="imagen" class="form-control" required>
        </div>
        <div class="mb-3">
          <label class="form-label">Título</label>
          <input type="text" name="titulo" class="form-control" placeholder="Ej: Promoción de verano">
        </div>
        <div class="mb-3">
          <label class="form-label">Descripción</label>
          <textarea name="descripcion" class="form-control" rows="2" placeholder="Ej: Descuento del 20% en todos los productos."></textarea>
        </div>
        <button type="submit" class="btn btn-primary">📤 Subir Publicidad</button>
      </form>
    </div>

    <!-- Sección de Publicidades -->
    <% if (publicidades.length) { %>
      <div class="row">
        <% publicidades.forEach(pub => { %>
          <div class="col-md-4 mb-4">
            <div class="card card-publicidad">
              <img src="/uploads/<%= pub.imagen %>" class="card-img-top" alt="Publicidad">
              <div class="card-body">
                <h5 class="card-title"><%= pub.titulo || 'Sin título' %></h5>
                <p class="card-text"><%= pub.descripcion || 'Sin descripción' %></p>
                <form action="/admin/publicidad/eliminar/<%= pub._id %>" method="POST">
                  <button class="btn btn-danger btn-sm">🗑️ Eliminar</button>
                </form>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <p class="text-muted">No hay elementos de publicidad actualmente.</p>
    <% } %>

  </div>

  <!-- JS Bootstrap -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>
